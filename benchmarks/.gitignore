# Local benchmark output lives under benchmarks/<env>.{env,http.json,
# bench.txt,metrics.json,bench.md} and is regenerated by every run.
# Keep the directory itself tracked so contributors can run the bench
# procedure without first creating the folder.
benchmarks/*
!benchmarks/.gitignore
