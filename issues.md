# temporal plot
- contrl+left create a point where we put linear lines in the plot, if the mayority of point is lower than the line we filter all points over that line, if the majority is higher than the line we filter out under that line (thus we can cerate adaptive filters to clean the data), all other plots should use the filterd data! NEW FEATURE
- colors for the traces are selectable in the filter menu and shown in the column selection thing too
# scatter plot

# desity plot
- the plot is not updated when selecting a new column combination
# upload
- ability to slect a subset of column via the table NEW FEATURE