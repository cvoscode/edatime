# ai/frontend/src/chart/gridLayout.test.md
> Unit tests for `computeChartGrid` and `scaleGridLayout`.

## Tests
- `computeChartGrid` — shrinks left gutter for compact Y labels; expands when Y-axis title is present.
