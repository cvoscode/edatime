# frontend/src/app/pageLifecycle.test.ts
> Tests for page lifecycle management.

## Tests
- `createPageLifecycle registers handler and returns unregister`
- `init fires on first visible page activation`
- `onEveryPageChange fires on every page change`
- `onVisible only fires for target page`
- `cleanup removes listener on unregister`
