name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Check formatting
        run: cargo fmt --check

      - name: Compile all targets
        run: cargo check --all-targets

      - name: Run tests
        run: cargo test

      - name: Check frontend syntax
        run: npm run check:frontend

      - name: Check frontend architecture
        run: npm run check:frontend:arch

      - name: Check frontend source reachability
        run: npm run check:frontend:reachability

      - name: Check repository hygiene
        run: npm run check:repo-hygiene

      - name: Check API contract coverage
        run: npm run check:api-contract

      - name: Check generated API reference
        run: npm run check:api-docs

      - name: Check backend ownership and stale surfaces
        run: npm run check:backend-hygiene

      - name: Compile benchmark harness
        run: cargo bench --workspace --no-run
